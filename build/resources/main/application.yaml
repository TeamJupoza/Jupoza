spring:
  datasource:
    # url
    url: jdbc:mysql://localhost:3306/jupoza?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    # user
    username: root
    # password
    password :
    # mysql-driver connector
    driver-class-name: com.mysql.cj.jdbc.Driver
